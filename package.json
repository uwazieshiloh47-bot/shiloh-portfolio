{
  "name": "shiloh-portfolio",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "test": "node --test",
    "test:smoke": "playwright test",
    "test:layout": "node scripts/check-layout.mjs",
    "check": "npm test && node --check visitor-counter.js && node --check architecture-animation.js && node --check scripts/serve.mjs && node --check scripts/check-layout.mjs"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "@playwright/test": "^1.62.1"
  }
}
